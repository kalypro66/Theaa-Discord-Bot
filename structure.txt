===== src/commands/utility/ping.js =====
const { SlashCommandBuilder } = require("discord.js");

module.exports = {

    data: new SlashCommandBuilder()
        .setName("ping")
        .setDescription("Replies with Pong!"),

    async execute(interaction) {


===== src/commands/utility/help.js =====
const {
    SlashCommandBuilder,
    EmbedBuilder
} = require('discord.js');

module.exports = {
    data: new SlashCommandBuilder()
        .setName('help')
        .setDescription('Shows all commands'),


===== src/commands/utility/setprefix.js =====
const {
    SlashCommandBuilder,
    PermissionFlagsBits
} = require("discord.js");

const {
    getPrefix,
    setPrefix
} = require("../../discord/prefixManager");


===== src/commands/information/serverinfo.js =====
const {
    SlashCommandBuilder,
    EmbedBuilder
} = require("discord.js");

module.exports = {

    data: new SlashCommandBuilder()
        .setName("serverinfo")
        .setDescription("Shows information about the server"),

===== src/commands/information/userinfo.js =====
const {
    SlashCommandBuilder,
    EmbedBuilder
} = require('discord.js');

module.exports = {
    data: new SlashCommandBuilder()
        .setName('userinfo')
        .setDescription('Shows information about a user')
        .addUserOption(option =>

===== src/commands/information/avatar.js =====
const {
    SlashCommandBuilder,
    EmbedBuilder
} = require('discord.js');

module.exports = {
    data: new SlashCommandBuilder()
        .setName('avatar')
        .setDescription('Shows a user avatar')
        .addUserOption(option =>

===== src/commands/information/banner.js =====
const {
    SlashCommandBuilder,
    EmbedBuilder
} = require('discord.js');

module.exports = {
    data: new SlashCommandBuilder()
        .setName('banner')
        .setDescription('Shows a user banner')
        .addUserOption(option =>

===== src/commands/moderation/removerole.js =====
const {
    SlashCommandBuilder,
    PermissionFlagsBits,
    EmbedBuilder
} = require('discord.js');

const { sendLog } = require('../../utils/logger');

module.exports = {
    data: new SlashCommandBuilder()

===== src/commands/moderation/kick.js =====
const {
    SlashCommandBuilder,
    PermissionFlagsBits,
    EmbedBuilder
} = require('discord.js');

module.exports = {
    data: new SlashCommandBuilder()
        .setName('kick')
        .setDescription('Kicks a member from the server')

===== src/commands/moderation/ban.js =====
const {
    SlashCommandBuilder,
    PermissionFlagsBits,
    EmbedBuilder
} = require('discord.js');

module.exports = {
    data: new SlashCommandBuilder()
        .setName('ban')
        .setDescription('Bans a member from the server')

===== src/commands/moderation/unban.js =====
const {
    SlashCommandBuilder,
    PermissionFlagsBits,
    EmbedBuilder
} = require('discord.js');

module.exports = {
    data: new SlashCommandBuilder()
        .setName('unban')
        .setDescription('Unbans a user from the server')

===== src/commands/moderation/timeout.js =====
const {
    SlashCommandBuilder,
    PermissionFlagsBits,
    EmbedBuilder
} = require('discord.js');

module.exports = {
    data: new SlashCommandBuilder()
        .setName('timeout')
        .setDescription('Timeouts a member')

===== src/commands/moderation/warn.js =====
const {
    SlashCommandBuilder,
    PermissionFlagsBits,
    EmbedBuilder
} = require('discord.js');

const fs = require('node:fs');
const path = require('node:path');

const { sendLog } = require('../../utils/logger');

===== src/commands/moderation/setlogs.js =====
const {
    SlashCommandBuilder,
    PermissionFlagsBits,
    EmbedBuilder
} = require('discord.js');

const fs = require('node:fs');
const path = require('node:path');

const logsPath = path.join(

===== src/commands/moderation/purge.js =====
const {
    SlashCommandBuilder,
    PermissionFlagsBits,
    EmbedBuilder
} = require('discord.js');

const { sendLog } = require('../../utils/logger');

module.exports = {
    data: new SlashCommandBuilder()

===== src/commands/moderation/nuke.js =====
const {
    SlashCommandBuilder,
    PermissionFlagsBits,
    EmbedBuilder,
    ActionRowBuilder,
    ButtonBuilder,
    ButtonStyle,
    ChannelType
} = require('discord.js');


===== src/commands/moderation/lock.js =====
const {
    SlashCommandBuilder,
    PermissionFlagsBits,
    EmbedBuilder
} = require('discord.js');

const { sendLog } = require('../../utils/logger');

module.exports = {
    data: new SlashCommandBuilder()

===== src/commands/moderation/unlock.js =====
const {
    SlashCommandBuilder,
    PermissionFlagsBits,
    EmbedBuilder
} = require('discord.js');

const { sendLog } = require('../../utils/logger');

module.exports = {
    data: new SlashCommandBuilder()

===== src/commands/moderation/addrole.js =====
const {
    SlashCommandBuilder,
    PermissionFlagsBits,
    EmbedBuilder
} = require('discord.js');

const { sendLog } = require('../../utils/logger');

module.exports = {
    data: new SlashCommandBuilder()

===== src/commands/moderation/automod.js =====
const {
    SlashCommandBuilder,
    PermissionFlagsBits,
    EmbedBuilder
} = require('discord.js');

const fs = require('fs');
const path = require('path');

const filePath = path.join(__dirname, '../../data/automod.json');

===== src/commands/ai/chat.js =====
const {
    SlashCommandBuilder
} = require("discord.js");

const askAI = require("../../ai/manager");

module.exports = {

    data: new SlashCommandBuilder()
        .setName("chat")

===== src/events/aiMessage.js =====
const messageRouter = require("../router/messageRouter");
const askAI = require("../ai/manager");
const getServerContext = require("../discord/serverContext");
const { getPrefix } = require("../discord/prefixManager");

module.exports = {
    name: "messageCreate",

    async execute(message) {


===== src/events/messagecreate.js =====
const {
    PermissionFlagsBits,
    EmbedBuilder
} = require('discord.js');

const fs = require('fs');
const path = require('path');

const automodPath = path.join(__dirname, '../data/automod.json');
const logsPath = path.join(__dirname, '../config/logs.json');

===== src/handlers/commandHandler.js =====
const {
    Collection
} = require("discord.js");

const fs = require("node:fs");
const path = require("node:path");

module.exports = client => {

    client.commands = new Collection();

===== src/handlers/eventHandler.js =====
const fs = require("node:fs");
const path = require("node:path");

module.exports = client => {

    const eventsPath =
        path.join(__dirname, "..", "events");

    const eventFiles =
        fs.readdirSync(eventsPath)

===== src/utils/logger.js =====
const fs = require('node:fs');
const path = require('node:path');

const logsPath = path.join(
    __dirname,
    '../config/logs.json'
);

async function sendLog(interaction, embed) {
    let logs = {};

===== src/config/logs.json =====
{
    "1528678283200303226": "1529266920740884561"
}
===== src/data/automod.json =====
{
    "1528678283200303226": {
        "invites": true,
        "links": true,
        "spam": true,
        "mentions": true,
        "everyone": true
    }
}
===== src/data/prefixes.json =====
{
    "1528678283200303226": "?"
}
===== src/router/messageRouter.js =====
const askAI = require("../ai/manager");

const {
    findCommand
} = require("./commandRegistry");

const executeCommand =
    require("./executeCommand");

module.exports = async function messageRouter(

===== src/router/commandRegistry.js =====
const fs = require("node:fs");
const path = require("node:path");

const registry = new Map();

function loadCommands() {

    registry.clear();

    const commandsPath =

===== src/router/executeCommand.js =====
module.exports = async function executeCommand(
    message,
    command
) {

    const args = message.content
        .trim()
        .split(/\s+/)
        .slice(1);


===== src/ai/openrouter.js =====
const { buildMessages } = require("./context");
const { addMessage } = require("./memory");

const MODELS = [
    "deepseek/deepseek-chat-v3-0324:free",
    "qwen/qwen3-235b-a22b:free",
    "google/gemma-3-27b-it:free"
];

async function askOpenRouter(context) {

===== src/ai/groq.js =====
const { buildMessages } = require("./context");
const { addMessage } = require("./memory");

const MODELS = [
"llama-3.3-70b-versatile",
"qwen/qwen3-32b",
"deepseek-r1-distill-llama-70b"
];

async function askGroq(context) {

===== src/ai/prompt/index.js =====
const identity = require("./identity");
const personality = require("./personality");
const commands = require("./commands");
const permissions = require("./permissions");
const discord = require("./discord");
const examples = require("./examples");

module.exports = [
    identity,
    personality,

===== src/ai/prompt/identity.js =====

===== src/ai/prompt/personality.js =====

===== src/ai/prompt/commands.js =====

===== src/ai/prompt/permissions.js =====

===== src/ai/prompt/discord.js =====

===== src/ai/prompt/examples.js =====

===== src/ai/future/aiLogger.js =====

===== src/ai/future/helpers.js =====

===== src/ai/future/intent.js =====

===== src/ai/future/moods.js =====

===== src/ai/future/stats.js =====

===== src/ai/context.js =====
const prompt = require("./prompt");
const { getHistory } = require("./memory");

function buildMessages(context) {

    const history = getHistory(context.guildId);

    const messages = [];

    messages.push({

===== src/ai/personality.js =====

===== src/ai/gemini.js =====
const { GoogleGenAI } = require("@google/genai");

const { buildMessages } = require("./context");
const { addMessage } = require("./memory");

const ai = new GoogleGenAI({
    apiKey: process.env.GEMINI_API_KEY
});

async function askGemini(context) {

===== src/ai/manager.js =====
const providerManager = require("./providerManager");

async function askAI(context) {

    try {

        const result =
            await providerManager(context);

        if (result.success) {

===== src/ai/memory.js =====
const memory = new Map();

const MAX_HISTORY = 50;

function getHistory(guildId) {

    if (!memory.has(guildId)) {
        memory.set(guildId, []);
    }


===== src/ai/providerManager.js =====
const askGemini = require("./gemini");
const askGroq = require("./groq");
const askOpenRouter = require("./openrouter");

const providers = [
    {
        name: "gemini",
        priority: 1,
        cooldownUntil: 0,
        ask: askGemini

===== src/discord/memberContext.js =====
const { PermissionsBitField } = require("discord.js");

function getMemberContext(member) {

    if (!member) return null;

    const roles = member.roles.cache
        .filter(role => role.id !== member.guild.id)
        .sort((a, b) => b.position - a.position)
        .map(role => role.name);

===== src/discord/serverContext.js =====
const getMemberContext = require("./memberContext");

async function getServerContext(message) {

    const guild = message.guild;

    const me = guild.members.me;

    const author = message.member;


===== src/discord/prefixManager.js =====
const fs = require("node:fs");
const path = require("node:path");

const PREFIX_FILE = path.join(
    __dirname,
    "..",
    "data",
    "prefixes.json"
);


===== data/automod.json =====
{}
===== data/warnings.json =====
{
    "1528678283200303226": {
        "720351927581278219": [
            {
                "reason": "cuz hes a dumbfuck",
                "moderator": "770202235741011989",
                "timestamp": 1784675633977
            },
            {
                "reason": "No reason provided",

